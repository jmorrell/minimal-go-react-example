# minimal-go-react-example

A minimal go example that uses esbuild to bundle JS and CSS for a React app with minimal configuration. There is also a go server to serve the built static assets plus some stubs for API endpoints.

See `Makefile` for entrypoints.
